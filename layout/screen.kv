<ScreenOne>:
    BoxLayout:
    	canvas:
			Color: 
				rgba: 1, 0, 0, 1
	        Rectangle:
	            pos: self.pos
	            # pos: self.pos
	            size: self.width, self.height
        BoxLayout:
        	size_hint: 0.5, 1
        	canvas:
    			Color: 
    				rgba: 0, 0, 1, 1
    	        Rectangle:
    	            pos: self.pos
    	            size: self.width, self.height
        	Button:
        		text: "Start"
        		on_press: root.printVars()
        	Button:
        		text: "Cancel"
        		on_press: root.cancel_delay()
	    BoxLayout:
	    	canvas:
				Color: 
					rgba: 0, 1, 0, 1
		        Rectangle:
		            pos: self.pos
		            # pos: self.pos
		            size: self.width, self.height
	    	Label:
	    		text: "My Label"